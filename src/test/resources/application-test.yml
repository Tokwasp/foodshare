spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
# Gemini API 설정 (test 프로파일 - 더미 값, 실제 API 호출 없음)
gemini:
  api-url: http://dummy
  api-key: dummy

gms:
  key: dummy

claude:
  api-key: dummy
